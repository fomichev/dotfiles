#!/bin/bash

MYDIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}" )" &>/dev/null && pwd)
source $MYDIR/../host_lib.sh

cd $KDIR

mkdir -p $KDIR/.claude
ln -fsn ~/src/dotfiles/kernel/skills $KDIR/.claude/skills

claude --effort max
