#!/bin/bash

MYDIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}" )" &>/dev/null && pwd)
source $MYDIR/../host_lib.sh

TOPDIR=$(git rev-parse --show-toplevel)

cd $TOPDIR

mkdir -p $TOPDIR/.claude
ln -fsn ~/src/dotfiles/kernel/skills $TOPDIR/.claude/skills

claude --effort max "$@"
