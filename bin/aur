#!/bin/bash

if [ $# -eq 0 ]; then
	set update orphaned foreign
fi

foreign() {
	pacman -Qm
}

orphaned() {
	sudo pacman -Rs $(pacman -Qqtd)
}

update() { yay; }

for f in "$@"; do
	echo "$f" | colorify
	$f
done
