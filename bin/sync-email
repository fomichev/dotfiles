#!/bin/bash

set -x
set -e

[ -e ~/.imapfilter.lua ] && imapfilter -c ~/.imapfilter.lua
[ -e ~/.mbsyncrc ] && mbsync -aV
[ -e ~/.notmuch-config ] && notmuch new
[ -d ~/src/public-inbox ] && exec ~/src/public-inbox/lei.sh up --all
[ -e ~/.l2mdconfig ] && exec $HOME/opt/sysroot/bin/l2md --verbose
