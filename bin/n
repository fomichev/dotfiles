#!/bin/sh

DIR=$HOME/notes

case "$1" in
	"-l")
		(cd $DIR && find . -name '*.txt' | sed -e 's@^\./@@' -e 's@.txt$@@')
		;;
	"--push")
		rclone sync -v ~/notes remote:Notes
		;;
	"--pull")
		rclone sync -v remote:Notes ~/notes
		;;
	*)
		$EDITOR "$DIR/${@}.txt"
		;;
esac
