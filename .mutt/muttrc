set from = "kernel@fomichev.me"

set alias_file = "~/.mutt/aliases"
source ~/.mutt/settings

set spoolfile = +inbox
set record = +inbox
set mbox = +inbox
set postponed = +drafts

mailboxes +inbox +archive +drafts +later
save-hook . +archive
