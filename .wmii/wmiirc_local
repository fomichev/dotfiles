MODKEY=Mod4
WMII_TERM='urxvt -e bash -c "tmux -q has-session && exec tmux attach-session -d || exec tmux new-session -s \"$USER@$(hostname)\""'
WMII_FONT='xft:Monospace-8'

status(){
	echo -n $(conky) $(xkblayout-state print "%s")
}
