set from = "kernel@fomichev.me"

# imap setup
set imap_user = "kernel@fomichev.me"

set smtp_url = "smtp://kernel@fomichev.me@smtp.gmail.com:587/"
set folder = "imaps://imap.gmail.com:993/"
set spoolfile = "+INBOX"
set postponed = "+[Gmail]/Черновики"
set record="=INBOX"

mailboxes =INBOX

set alias_file = "~/.mutt/aliases"
source $alias_file

source ~/.mutt/misc

source ~/.mutt/pwd
